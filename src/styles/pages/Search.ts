import styled from '@emotion/styled';

import { common } from '../theme/common';

export const StyledSearch = styled.body`
  background: ${common.color.white};
  color: ${common.color.black};
  height: 100vh;

  main {
    margin-top: 66px;
  }
`;
